spring.application.name=user-ws
server.port=0
eureka.client.serviceUrl.defaultZone=http://localhost:8010/eureka
spring.devtools.restart.enabled=true
#spring.h2.console.enabled=true
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#spring.h2.console.settings.web-allow-others=true
logging.level.org.springframework.security=DEBUG




token.expiration_time = 3600000
token.secret = zv7)3&-db408u(et_@6w0xanh6%zgx#$jx4$0%$8whr_g=!#(%
gateway.ip=127.0.0.1


spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

management.endpoint.health.enabled=true
management.endpoint.health.show-details=always
management.health.circuitbreakers.enabled=true
logging.level.com.manash.photoapp.api.users.PhotoAppApiUsers.data.AlbumsServiceClient=DEBUG


# Sets the failure rate threshold (in %) that triggers the circuit breaker to open
resilience4j.circuitbreaker.instances.albumsServiceCB.failure-rate-threshold=50

# Configures an automatic transition from open to half-open state
resilience4j.circuitbreaker.instances.albumsServiceCB.automatic-transition-from-open-to-half-open-enabled=true

# Specifies the wait duration in the open state before transitioning to half-open state
resilience4j.circuitbreaker.instances.albumsServiceCB.wait-duration-in-open-state=10000ms

# Configures the sliding window type (count-based or time-based)
resilience4j.circuitbreaker.instances.albumsServiceCB.sliding-window-type=COUNT_BASED

# Sets the sliding window size (number of calls to evaluate the error rate)
resilience4j.circuitbreaker.instances.albumsServiceCB.sliding-window-size=2

# Configures the minimum number of calls needed in a sliding window to calculate the error rate
resilience4j.circuitbreaker.instances.albumsServiceCB.minimum-number-of-calls=1

# Configures the buffer size for event consumers, helpful for monitoring recent events
resilience4j.circuitbreaker.instances.albumsServiceCB.event-consumer-buffer-size=10


logging.file.name=user-ws.log